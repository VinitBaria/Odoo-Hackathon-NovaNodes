<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Item - ReWear</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-green-50 to-blue-50">
    <nav class="bg-green-700 sticky top-0 z-50 shadow-lg">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
            <a href="/" class="text-3xl font-extrabold text-white tracking-tight">ReWear</a>
            <div class="space-x-6">
                <a href="/dashboard" class="text-white hover:text-green-200 transition">Dashboard</a>
                <a href="/logout" class="text-white hover:text-green-200 transition">Logout</a>
            </div>
        </div>
    </nav>
    <div class="container mx-auto px-4 py-12">
        <h1 class="text-4xl font-bold text-gray-800 mb-6 fade-in">Add New Item</h1>
        <form action="/add-item" method="POST" class="bg-white p-8 rounded-lg shadow-xl max-w-2xl mx-auto fade-in">
            <div class="mb-6">
                <label class="block mb-2 text-gray-700 font-medium">Title</label>
                <input type="text" name="title" class="w-full border border-gray-300 p-3 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 transition" required>
            </div>
            <div class="mb-6">
                <label class="block mb-2 text-gray-700 font-medium">Description</label>
                <textarea name="description" class="w-full border border-gray-300 p-3 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 transition" rows="5" required></textarea>
            </div>
            <div class="mb-6">
                <label class="block mb-2 text-gray-700 font-medium">Category</label>
                <select name="category" class="w-full border border-gray-300 p-3 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 transition" required>
                    <option value="Tops">Tops</option>
                    <option value="Bottoms">Bottoms</option>
                    <option value="Dresses">Dresses</option>
                    <option value="Accessories">Accessories</option>
                </select>
            </div>
            <div class="mb-6">
                <label class="block mb-2 text-gray-700 font-medium">Type</label>
                <input type="text" name="type" class="w-full border border-gray-300 p-3 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 transition" required>
            </div>
            <div class="mb-6">
                <label class="block mb-2 text-gray-700 font-medium">Size</label>
                <input type="text" name="size" class="w-full border border-gray-300 p-3 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 transition" required>
            </div>
            <div class="mb-6">
                <label class="block mb-2 text-gray-700 font-medium">Condition</label>
                <select name="condition" class="w-full border border-gray-300 p-3 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 transition" required>
                    <option value="New">New</option>
                    <option value="Like New">Like New</option>
                    <option value="Good">Good</option>
                    <option value="Fair">Fair</option>
                </select>
            </div>
            <div class="mb-6">
                <label class="block mb-2 text-gray-700 font-medium">Tags (comma-separated)</label>
                <input type="text" name="tags" class="w-full border border-gray-300 p-3 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 transition">
            </div>
            <button type="submit" class="w-full bg-green-600 text-white p-3 rounded-lg hover:bg-green-700 transition transform hover:-translate-y-1 shadow-lg">Submit</button>
        </form>
    </div>
</body>
</html>